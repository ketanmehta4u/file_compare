node_modules/
frontend/dist/
backend/dist/
.angular/cache/
*.log
.env
coverage/

# Prompts and session context for AI-assisted work: kept locally, never
# published. The glob covers future ones too, so a new prompt file cannot
# be committed by accident.
CLAUDE.md
*prompt*.md
*PROMPT*.md
